node_modules
.next
apps/web/.prisma
.env
.env.local
.env.*.local
dist
build
*.tsbuildinfo
.vercel
.turbo
prisma/migrations/dev

# Local uploads (api/upload writes here in dev)
apps/web/public/uploads
public/uploads

# OS
.DS_Store
Thumbs.db

# Logs (root-level only; app routes like projects/logs are not ignored)
/logs
*.log
npm-debug.log*
pnpm-debug.log*

# Local editor/IDE config (may contain secrets)
.claude

# Editor
.vscode
.idea
*.swp
*.swo

# Playwright (local runs)
apps/web/playwright-report
apps/web/test-results
